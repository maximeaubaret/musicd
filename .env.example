# Jellyfin Configuration
JELLYFIN_URL=http://localhost:8096

# Daemon Configuration
DAEMON_PORT=3000
DAEMON_HOST=127.0.0.1

# Audio Configuration
AUDIO_DEVICE=default

# Note: Authentication is managed via 'bun run cli setup'
# Your credentials are stored securely in .jellyfin-auth.json
